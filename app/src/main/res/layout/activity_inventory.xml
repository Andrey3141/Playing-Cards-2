<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_gradient"
    android:animateLayoutChanges="true">

    <!-- Декоративные карточки на фоне (как в главном меню) -->
    <ImageView
        android:id="@+id/decorCard1"
        android:layout_width="120dp"
        android:layout_height="160dp"
        android:src="@drawable/card_back"
        android:rotation="-15"
        android:alpha="0.15"
        android:translationX="-40dp"
        android:translationY="-50dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/decorCard2"
        android:layout_width="120dp"
        android:layout_height="160dp"
        android:src="@drawable/card_back"
        android:rotation="25"
        android:alpha="0.15"
        android:translationX="50dp"
        android:translationY="100dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Стеклянная карточка (эффект glass morphism) -->
    <androidx.cardview.widget.CardView
        android:id="@+id/glassCard"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="20dp"
        app:cardCornerRadius="32dp"
        app:cardElevation="20dp"
        app:cardBackgroundColor="#B3FFFFFF"
        app:cardMaxElevation="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Основной контент -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="20dp"
        android:clipChildren="false"
        android:clipToPadding="false">

        <!-- Верхняя панель с заголовком и кнопкой назад -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp">

            <!-- Кнопка назад (красивая, как в главном меню) -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/backButton"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_alignParentStart="true"
                app:cardCornerRadius="28dp"
                app:cardElevation="8dp"
                app:strokeWidth="2dp"
                app:strokeColor="#FFD700"
                android:foreground="?android:attr/selectableItemBackground"
                app:cardBackgroundColor="#FF5722">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:text="←"
                    android:textSize="28sp"
                    android:textColor="#FFFFFF"
                    android:textStyle="bold"
                    android:gravity="center"
                    android:fontFamily="@font/montserrat_bold" />

            </com.google.android.material.card.MaterialCardView>

            <!-- Заголовок -->
            <TextView
                android:id="@+id/titleText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerHorizontal="true"
                android:text="ИНВЕНТАРЬ"
                android:textSize="32sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                android:fontFamily="@font/playfair_display_bold"
                android:letterSpacing="0.1"
                android:shadowColor="#80000000"
                android:shadowDx="2"
                android:shadowDy="2"
                android:shadowRadius="4"
                android:layout_marginTop="8dp" />

        </RelativeLayout>

        <Space
            android:layout_width="match_parent"
            android:layout_height="20dp" />

        <!-- Табы для категорий (красивые, как в главном меню) -->
        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabLayout"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="@drawable/tab_background"
            app:tabTextColor="#80FFFFFF"
            app:tabSelectedTextColor="#FFFFFF"
            app:tabIndicatorColor="#FFD700"
            app:tabIndicatorHeight="3dp"
            app:tabMode="fixed"
            app:tabGravity="fill"
            app:tabTextAppearance="@style/TabTextStyle"
            app:tabRippleColor="@android:color/transparent" />

        <Space
            android:layout_width="match_parent"
            android:layout_height="16dp" />

        <!-- Название текущей категории с иконкой -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="16dp">

            <ImageView
                android:id="@+id/categoryIcon"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="@drawable/ic_deck"
                android:tint="#FFD700"
                android:layout_marginEnd="8dp" />

            <TextView
                android:id="@+id/categoryTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Моя колода"
                android:textSize="24sp"
                android:textColor="#FFFFFF"
                android:fontFamily="@font/playfair_display_bold" />

        </LinearLayout>

        <!-- RecyclerView для карточек с красивым оформлением -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:orientation="horizontal"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            android:clipToPadding="false"
            android:paddingStart="4dp"
            android:paddingEnd="4dp" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>